# MoonBit 国产基础软件开源大赛 - 项目申报书

## 1. 项目基本信息
- **项目名称**：MoonBit 生存分析工具箱 (moon-survival)
- **项目方向**：MoonBit 数据科学与统计算法库
- **GitLink 仓库**：[https://gitlink.org.cn/ylyl/moon-survival](https://gitlink.org.cn/ylyl/moon-survival)
- **GitHub 仓库**：[https://github.com/Lyl66655/moon-survival](https://github.com/Lyl66655/moon-survival)
- **贡献者**：ylyl (账号创建者)

## 2. 项目概述
`moon-survival` 是 MoonBit 生态中首个原生的生存分析（Survival Analysis）工具箱。生存分析在众多学科和工程领域中扮演着重要角色，主要用于分析和预测某个事件发生的时间。由于现实中数据往往是不完整的（例如患者失访、设备在观察期结束时仍未损坏等），此类“右删失（Right-Censored）”数据的处理具有较高的统计学门槛。

本项目填补了 MoonBit 语言在高级统计算法领域的空白，通过提供易于使用、内存安全且高效的统计预测算法，极大丰富了 MoonBit 生态，为未来 MoonBit 进入医疗健康、可靠性工程、商业数据分析等领域打下坚实的基础。

## 3. 应用场景
1. **医疗随访与临床试验**：评估新药或新疗法对患者生存时间的影响。
2. **设备寿命与可靠性工程**：预测工业设备、服务器硬盘或电子元器件的平均无故障时间（MTBF），制定合理的维护策略。
3. **软件故障分析**：评估软件系统从上线到发生首次严重崩溃的时间分布。
4. **用户流失（Churn）分析**：在互联网商业运营中，预测用户从注册到最终注销或流失的时间周期，帮助制定用户留存策略。

## 4. 核心功能与技术实现
本项目完全使用 MoonBit (v0.10.3) 编写，具有以下核心特性：
- **数据结构支持**：原生支持处理右删失（Right-Censoring）的时间-事件数据，内置排序与预处理能力。
- **Kaplan-Meier 估计器**：实现了非参数的 Kaplan-Meier 生存曲线拟合，能够预测任意时间点的生存概率，并采用 Greenwood 公式计算了方差和置信区间的标准误。
- **Nelson-Aalen 估计器**：实现了累计风险（Cumulative Hazard）的非参数估计。
- **Cox 比例风险模型 (Cox Proportional Hazards)**：构建了处理带协变量（Covariates）的 Cox 模型的底层基础，支持计算偏对数似然（Partial Likelihood），为进一步引入特征回归和梯度下降求解器奠定了基础。
- **数学基础支持**：在工具箱内部提供了轻量级的牛顿迭代开方和泰勒展开指数计算，以减少对特定后端的依赖，实现最大程度的跨端兼容性（Wasm/Native/JS 等）。

## 5. 项目亮点与创新性
- **生态独创性**：经过在 mooncakes.io 的检索，目前生态内不存在高度重合的工具，项目属于从 0 到 1 的底层数学/统计类基础设施建设。
- **扩展性**：良好的架构设计为未来引入 Competing Risks（竞争风险模型）和 Time-varying Covariates（随时间变化的协变量）预留了接口。
- **严格的代码质量**：全面配置了 MoonBit 官方推荐的 CI/CD 工作流，确保 `moon check__, `moon test` 均绿灯通过，并且强制执行 `moon fmt --deny-warn` 和 `moon info --deny-warn`，代码风格统一规范。

## 6. 后续演进计划 (Expected Deliverables & Roadmaps)
1. **完善 Cox 模型求解器**：引入 L-BFGS 或 Newton-Raphson 优化器来最大化偏似然函数，实现协变量权重的自动拟合。
2. **扩展数据导入能力**：结合 `@json` 或 CSV 解析库，支持直接从数据文件中加载生存数据框（DataFrames）。
3. **可视化适配**：未来结合 MoonBit 的图形库，输出生存曲线的可视化图表，打造类似 R 语言 `survival` 包的一站式分析体验。
4. **发布至 Mooncakes**：确保各项指标和文档达到生产级别后，正式发布至 mooncakes.io。
